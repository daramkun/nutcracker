# 크로스 레이아웃 검증 케이스

동일한 단어를 각 레이아웃으로 입력했을 때 동일한 출력이 나와야 한다.

## 9-1. 검증 단어: "한글"

| 레이아웃    | 입력 시퀀스                                  | 기대 출력 |
|-------------|----------------------------------------------|-----------|
| 두벌식      | `G`(ㅎ) `K`(ㅏ) `S`(ㄴ) `R`(ㄱ) `L`(ㅣ)   | 한글      |
| 단모음      | `G`(ㅎ) `J`(ㅏ) `S`(ㄴ) `R`(ㄱ) `K`(ㅣ)   | 한글      |
| 천지인      | `8``8`(ㅎ) `ㅣ``ㆍ`(ㅏ) `5`(ㄴ) `4`(ㄱ) `1`(ㅣ) | 한글   |
| KT 나랏글   | `8`(ㅅ)`*``*``*`(ㅎ) `3`(ㅏ) `5`(ㄴ) `1`(ㄱ) `2``2`(ㅡ)`9`(ㅣ)=ㅢ? → `2`(ㅣ) | 한글 |
| SKY-II      | `0``0`(ㅎ) `3`(ㅏ) `5`(ㄴ) `1`(ㄱ) `2``2`(ㅡ→) `2`(ㅣ) | 한글   |
| 모토로라    | `0``0`(ㅎ) `3`(ㅏ) `2`(ㄴ) `1`(ㄱ) `9``9`(ㅡ)`9`(ㅣ)=ㅢ? → `9`(ㅣ) | 한글 |

---

## 9-2. 공통 Backspace 동작 검증

모든 레이아웃에서 아래 시퀀스는 동일하게 동작해야 한다:

| 단계 | 조작      | 화면 표시 | FSM 상태 |
|------|-----------|-----------|----------|
| 1    | ㄱ 입력   | ㄱ        | S1       |
| 2    | ㅏ 입력   | 가        | S2       |
| 3    | ㄴ 입력   | 간        | S3       |
| 4    | BS        | 가        | S2       |
| 5    | BS        | ㄱ        | S1       |
| 6    | BS        | (빈)      | S0       |
| 7    | BS        | (무시)    | S0       |

---

## 9-3. 겹받침 분리 검증 (공통)

모든 레이아웃에서 겹받침이 지원되는 경우 동일하게 동작해야 한다:

| 단계 | 조작    | 화면 표시 | FSM 상태  |
|------|---------|-----------|-----------|
| 1    | ㄷ 입력 | ㄷ        | S1        |
| 2    | ㅏ 입력 | 다        | S2        |
| 3    | ㄹ 입력 | 달        | S3        |
| 4    | ㄱ 입력 | 닭 (조합) | S3D       |
| 5    | ㅏ 입력 | 달가      | S2(새 글자) |
| 6    | BS      | 달        | S2(이전)  |

> 단모음/나랏글/모토로라 등 겹받침 입력이 우연히 발생할 수 있는 경우에도 [SPEC_COMMON.md의 1-6절](SPEC_COMMON.md#1-6-겹받침-조합-테이블) 테이블에 따라 동일하게 처리한다.

---

## 9-4. 단모음 이중모음 Backspace 검증

단모음에서 이중모음(`J``J` → ㅑ 등)은 전처리 단계에서 FSM에 단일 모음으로 전달된다.  
따라서 Backspace는 [SPEC_COMMON.md의 1-10절](SPEC_COMMON.md#1-10-backspace-동작-명세) 공통 규칙에 따라 **FSM 상태 기준으로 중성 단위로 삭제**한다.  
이중모음을 기본 모음으로 역분해하지 않는다.

| 단계 | 조작          | 화면 표시 | FSM 상태 | 설명                        |
|------|---------------|-----------|----------|-----------------------------|
| 1    | `J``J` (ㅑ)   | ㅑ        | S4       | 이중모음 전처리 후 FSM 진입 |
| 2    | BS            | (빈)      | S0       | 중성(ㅑ) 통째로 제거        |
| —    | —             | —         | —        |                             |
| 1    | `D` `J``J`    | 야        | S2       | ㅇ+ㅑ                       |
| 2    | BS            | ㅇ        | S1       | 중성(ㅑ) 통째로 제거        |
| 3    | BS            | (빈)      | S0       | 초성 제거                   |

---

## 검증 체크리스트

각 레이아웃별 구현 완료 시 다음을 검증하세요:

- [ ] 섹션 9-1: 모든 레이아웃에서 "한글" 입력 결과 동일
- [ ] 섹션 9-2: Backspace 7단계 모두 올바르게 동작
- [ ] 섹션 9-3: 겹받침 분리 및 복구 올바르게 동작
- [ ] 섹션 9-4: 단모음 이중모음 Backspace 정상 동작
- [ ] 각 레이아웃의 검증 케이스 (SPEC_LAYOUTS.md) 모두 통과

---

*이 문서는 [SPEC_COMMON.md](SPEC_COMMON.md)와 [SPEC_LAYOUTS.md](SPEC_LAYOUTS.md)의 내용을 보완하는 통합 테스트 가이드입니다.*